import React from "react";
import { Bar } from "react-chartjs-2";


const options = {

}

function InvestorsChart(props){

    const getData = () => {
        return ()
    }

    return(<Bar data={{labels : props.labels, datasets: [{label: }]}} options={options} >

    </Bar>)
}